"use strict";

// * The Background Image
const background = $("<img>")
  .addClass("background")
  .attr("src", "./assets/battle-background.png")
  .attr("alt", "Background");

// * Player 1
// ? Character Icon
const player1Icon = $("<img>").addClass("player-1-icon");

// ! Battle Buttons + Progress

// ? Name + Healthbar

// ? Movesets

// ? Abilities

// * Player 2
// ? Character Icon

// ! Battle Buttons + Progress

// ? Name + Healthbar

// ? Movesets

// ? Abilities

$("body").append(background);
